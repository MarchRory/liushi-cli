const path = require("path");
const fsExtra = require('fs-extra')
const { createArgsModel } = require('./types/index')

module.exports = async function (args: createArgsModel) {
    const { name, options } = args

}